from friction import friction #imported to help calculate the wheel force

class wheel:
    def setwheelforce(w):
        wheelforce = w;
    def getwheelforce:
        return wheelforce;
    def calcwheelforce:
        #this is where the forward force of a wheel will be calculated
