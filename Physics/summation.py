from force import force
from friction import friction
from gravity import gravity

class summation:
    xsum = float(0);
    ysum = float(0);
    zsum = float(0);
    def getxsum(x):
        xforce = x;
    def getysum(y):
        yforce = y;
    def getzsum(z):
        zforce = z;
    def calcxsum(x):
        #this is where the x component of the sum will be calculated
    def calcysum(y):
        #this is where the y component of the sum will be calculated
    def calczsum(z):
        #this is where the z component of the sum will be calculated
